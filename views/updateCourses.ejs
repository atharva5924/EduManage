<%- include('partials/header') %>

<div class="container mt-5">
  <h1>Update Course</h1>

  <div class="row">
    <div class="col-sm-12">
      <div class="card">
        <div class="card-body">
          <form action="/updateCourse" method="POST">
            <input type="hidden" name="course_id" value="<%= course.course_id %>">
            <div class="mb-3">
              <label for="course_name" class="form-label">Course Name</label>
              <input type="text" class="form-control" id="course_name" name="course_name" value="<%= course.course_name %>" required>
            </div>
            <div class="mb-3">
              <label for="department_id" class="form-label">Department ID</label>
              <input type="number" class="form-control" id="department_id" name="department_id" value="<%= course.department_id %>" required>
            </div>
            <button type="submit" class="btn btn-primary">Update Course</button>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>

<%- include('partials/footer') %>
